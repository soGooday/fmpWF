import "./index.scss"

let _config={
    el:"body"
}
export class HeadBox {
    constructor(config){
        this.config = Object.assign(_config,config);
        this.init(this.config)
    }
    init(config){
        this.init(config)
    }
    bin(){}

    render(){


    }
} 